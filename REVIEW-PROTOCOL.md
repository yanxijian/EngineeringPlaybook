# 工程设计评审协议

## 目的

本协议让方案设计者、实施者和评审员可以由不同的人或 AI 模型承担，仍保持可追溯、可复核的设计评审闭环。

## 产物与所有权

| 产物 | 创建者 | 可修改者 | 用途 |
| --- | --- | --- | --- |
| 设计/实施方案 | 设计者 | 设计者 | 描述目标、约束、设计、证据导航、验证与采纳回应 |
| 评审报告 | 评审员 | 评审员 | 独立记录当前轮问题、证据、风险和结论 |
| 验证记录 | 实施者 | 实施者 | 记录实际命令、结果、版本与残余风险 |

评审报告是独立评审证据。除非用户在当前任务中明确授权，设计者和实施者不得直接修改报告；评审员也不得修改方案或源代码。设计者只能在方案的“评审反馈与采纳说明”章节回应；评审员只能在评审报告中记录证据、问题和结论。注释、措辞润色、删除和格式调整同样受此所有权边界约束。

## 文件命名与发现

- 评审报告与被评审方案放在同一目录，文件名为 `<方案文件名去除扩展名>-review.md`。
- 例如 `feature-plan.md` 对应 `feature-plan-review.md`。
- 设计者收到“看下评审报告”“继续”或“再来”而没有明确路径时，先以当前方案路径推导同目录的 `-review.md`；找不到时再在同目录查找唯一的 `*-review.md`。存在多个候选时，必须请求用户指定，不能猜测。

## 固定流程

1. 设计者发布方案，标明版本、范围、非目标、验证方式和可供评审员快速核查的证据导航。
2. 评审员依据实际代码、文档、版本和可执行验证开展独立评审，不以方案中的说明或承诺作为事实依据；在同目录的 `-review.md` 中记录当前轮结论和带唯一 ID 的意见，例如 `P1-01`、`P2-03`、`P3-02`。
3. 设计者读取报告后，独立复核每项意见的代码/文档依据和推理。合理意见采纳；有疑问、异议或替代方案时，在方案的“评审反馈与采纳说明”章节清晰说明。
4. 设计者重新整理并生成完整方案，使正文和反馈说明一致；不得仅在文末堆叠补丁式文字或直接编辑评审报告。
5. 评审员重新依据当前方案和实际仓库事实评审，重点核查“评审反馈与采纳说明”，并直接重写同一个评审报告，仅保留当前有效发现与结论。
6. 在所有问题得到确认或回归前，用户的“继续”“再来”等指令表示当前角色重复其职责：设计者复核报告并重整方案；评审员独立复审并重写报告。该指令不授予角色切换权限。方案通过后才实施；实施完成后，由评审员做代码级复审并核对实际验证记录。

## 角色固定与扩展设计建议

设计者和评审员是互斥且在当前任务中固定的角色。一个 AI 不得因为流程进入下一步骤而自行切换角色、创建另一角色的产物或修改另一角色拥有的文件；只有用户在当前任务中明确重新委派角色时才能切换。

评审员可提出由实际核查直接支持的相邻设计改进，使用 `D-01` 格式并置于“扩展设计建议”章节。这类建议必须与目标、关键路径、安全性、兼容性、可运维性或验证能力直接相关，且说明依据、收益和实施影响。它们不得无依据扩大需求、流程或重构范围；除非独立演变为有证据的 P 类发现，否则不改变总体通过结论。设计者仍必须在方案中明确采纳、部分采纳、不采纳或需澄清。

## 方案证据导航

方案必须包含足以让评审员快速独立核查的导航：

- 仓库、分支或提交 SHA，以及适用的构建配置和平台。
- 相关源文件、文档、符号或配置项的路径；关键判断提供行号或可搜索的符号名。
- 已执行验证的精确命令和结果，或尚未执行时的明确说明。
- 外部依赖、公共接口和跨项目影响。

导航是检索入口，不是证据本身。评审员必须读取实际内容或运行合适的检查，不能仅复述方案。

## 事实分级

每份产物必须清楚区分：

- **已验证事实**：可指向当前代码、构建输出、测试结果或已固定的版本。
- **方案承诺**：尚未实现的设计、计划中的接入或预期验证。
- **风险/假设**：尚待确认的前提、兼容性风险或替代方案。

不得将方案承诺描述为已实现或已验证的事实。

## 意见状态

方案中的每条回应使用以下之一：`待处理`、`已采纳`、`部分采纳`、`不采纳`、`需澄清`。除“待处理”外，每项都必须说明独立核实结果、理由、方案落点和验证方式。P 类发现未回应为结构错误；D 类扩展设计建议未回应默认产生警告，项目 CI 可将其提升为错误。已关闭意见可在后续方案重整时简洁归纳，但不得掩盖仍有效的风险或异议。

## 通过标准

方案评审通过只表示可以实施，不表示功能已完成。代码级通过至少要求：实现与方案一致、针对性构建/测试通过、已知残余风险被记录。